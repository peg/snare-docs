---
title: stripe
description: Stripe CLI config canary.
---

The `stripe` canary plants a fake Stripe CLI configuration with a verify URL.

## Reliability: Medium

Fires when the Stripe CLI is invoked with the fake config or when an agent follows the verify URL. Less reliable than mechanism-based canaries — consider `awsproc` or `ssh` for primary coverage.
